name: electrolux
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
     sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  switcher_button: ^0.0.4

flutter:
  assets:
    - assets/
  uses-material-design: true


